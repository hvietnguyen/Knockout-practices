@model IEnumerable<Key_onboarding.Models.ProductSold>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
    <input class="btn btn-sm btn-success" type="button" value="Create New" onclick="create('/ProductSolds/Create')" />
</p>
<table class="table">
    <tr>
        <th>
            Customer Name @*@Html.DisplayNameFor(model => model.Customer.Name)*@
        </th>
        <th>
            Product Name @*@Html.DisplayNameFor(model => model.Product.Name)*@
        </th>
        <th>
            Store Name @*@Html.DisplayNameFor(model => model.Store.Name)*@
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateSold)
        </th>
        <th>Actions</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Store.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateSold)
        </td>
        <td>
            <input class="btn btn-sm btn-success" type="button" value="Edit" onclick="edit('/ProductSolds/Edit/'+@item.Id)" />
            <input class="btn btn-sm btn-danger" type="button" value="Delete" onclick="deleteItem('/ProductSolds/Delete/'+@item.Id)" />
            @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })*@
        </td>
    </tr>
}

</table>

<!-- Modals -->

<div id="myModal" class="modal fade" role="dialog">
</div>